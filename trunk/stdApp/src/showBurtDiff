#!/bin/csh
echo "< {$1}"
echo "> {$2}"
echo "---------------------------------------"
diff -w $1 $2 | awk '/Time:/ {print$0;next}; /[<>]/ {print $1" "$2" "$4" "$5" "$6" "$7" "$8" "$9" "$10}' | sort +1 -2
echo "---------------------------------------"
echo "< {$1}"
echo "> {$2}"
